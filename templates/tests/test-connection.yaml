apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "florestad-chart.fullname" . }}-test-connection"
  labels:
    {{- include "florestad-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "florestad-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
